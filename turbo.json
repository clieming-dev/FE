{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "mobile:build": {
      "cache": false,
      "inputs": ["apps/mobile/**"]
    }
  }
}
